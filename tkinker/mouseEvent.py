from tkinter import *
def dosomething(event):
    print("you did a thing")
window = Tk()
# window.bind("<Button-1>",dosomething) esquerda
# window.bind("<Button-2>",dosomething) scrool
# window.bind("<Button-3>",dosomething) right mouse click
# window.bind("<ButtonRelease>",dosomething) esquda direita
# window.bind("<Enter>",dosomething) enter 
# window.bind("<Leave>",dosomething) enter 
# window.bind("<Motion>",dosomething) where the mouse move 

window.mainloop()